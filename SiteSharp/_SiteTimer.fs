module _SiteTimer

open System.Net
open Microsoft.FSharp.Control.WebExtensions

(*
let internal TimeRequest (url: string) callback = 
   async {
     
      let request = WebRequest.Create(url)

      let! data = request.AsyncGetResponse()


      ()

   }
   *)